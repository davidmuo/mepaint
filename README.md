
# Digital Painting Application

A browser-based digital painting app with realistic pigment-based color mixing, layers, real-time collaboration, and animation support.

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.
